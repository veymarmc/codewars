const findRepeatedSequenceLength = require('./findRepeatedSequenceLength');

describe('Find the repeated sequence length:', () => {
  test('dumb test', () => {
    expect(findRepeatedSequenceLength(0)).toEqual(0);
  })
});
